rules_version = '2'; service cloud.firestore { match /databases/{database}/documents { match /portfolio/{item} { allow read: if resource.data.published == true || request.auth != null && request.auth.token.email in ['ADMIN_EMAIL']; allow write: if request.auth != null && request.auth.token.email in ['ADMIN_EMAIL']; } } }
